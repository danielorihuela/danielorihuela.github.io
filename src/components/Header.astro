---
const navItems = [
	{ name: "blog", path: "/", cmd: "blog" },
	{ name: "oss", path: "/open-source/", cmd: "contrib" },
];

const currentPath = Astro.url.pathname;
---

<header>
	<nav class="fixed top-0 left-0 right-0 z-50 bg-cyber-black/95 backdrop-blur-sm border-b border-cyber-border">
		<div class="max-w-6xl mx-auto px-4 lg:px-6">
			<div class="flex items-center justify-between h-14">
				{/* Terminal Prompt Logo */}
				<a
					href="/"
					class="font-display text-sm text-terminal-text hover:text-accent transition-colors duration-300 group"
				>
					<span class="text-accent font-bold">daniel</span><span class="text-terminal-text opacity-40">@</span><span class="text-terminal-bright">oss</span><span class="text-terminal-text opacity-40">:</span><span class="text-terminal-text">~</span>
				</a>

				{/* Desktop Navigation */}
				<div class="hidden md:flex items-center gap-1">
					{navItems.map((item) => (
						<a
							href={item.path}
							class={`font-display text-xs px-3 py-1.5 rounded transition-all duration-300 uppercase tracking-wider ${
								currentPath === item.path
									? "text-accent bg-accent/10 border border-accent/30 shadow-[0_0_10px_rgba(0,212,255,0.1)]"
									: "text-terminal-text opacity-60 hover:text-accent hover:bg-accent/5 hover:opacity-100"
							}`}
						>
							<span class="text-accent/40 mr-1 group-hover:text-accent group-hover:scale-110 transition-all duration-300">./</span>{item.cmd}
						</a>
					))}
				</div>

				{/* Mobile menu button */}
				<button
					id="mobile-menu-button"
					class="md:hidden text-terminal-text opacity-60 hover:text-accent hover:opacity-100 transition-all duration-300 font-display text-xs uppercase tracking-widest"
					aria-label="Toggle menu"
				>
					<span id="menu-label">[menu]</span>
				</button>
			</div>

			<div
				id="mobile-menu"
				class="md:hidden overflow-hidden transition-all duration-500 max-h-0 opacity-0"
			>
				<div class="py-2 border-t border-cyber-border/30">
					{navItems.map((item) => (
						<a
							href={item.path}
							class={`block font-display text-xs uppercase tracking-widest px-4 py-3.5 transition-all duration-300 ${
								currentPath === item.path
									? "text-accent bg-accent/10 border-l-2 border-accent"
									: "text-terminal-text opacity-60 hover:text-accent hover:bg-accent/5 hover:opacity-100"
							}`}
						>
							<span class="text-accent mr-3">_</span>{item.cmd}
						</a>
					))}
				</div>
			</div>
		</div>
	</nav>

	<script>
		document.addEventListener("DOMContentLoaded", () => {
			const btn = document.getElementById("mobile-menu-button");
			const label = document.getElementById("menu-label");
			if (!btn || !label) return;

			btn.addEventListener("click", () => {
				const menu = document.getElementById("mobile-menu");
				if (!menu) return;

				if (menu.classList.contains("max-h-0")) {
					menu.classList.remove("max-h-0", "opacity-0");
					menu.classList.add("max-h-96", "opacity-100");
					label.textContent = "[close]";
				} else {
					menu.classList.remove("max-h-96", "opacity-100");
					menu.classList.add("max-h-0", "opacity-0");
					label.textContent = "[menu]";
				}
			});
		});
	</script>
</header>
